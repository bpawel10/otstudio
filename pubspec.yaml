name: otstudio
description: A new Flutter project.

publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.15.0
  flutter_bloc: ^8.0.1
  bloc_concurrency: ^0.2.0
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
